-images
-aduio file
-programming fila
-python
	-pygame library
	-math
	-random
- VScode
------------------------------------
#ÇÓÊÏÚÇÁ ÇáãßÇÊÈ
import math
import random

import pygame
from pygame import mixer

#ÚãáíÉ ÈäÇÁ ßáÇÓ ÇááÚÈÉ
pygame.init()

#ÊÍÏíÏ ÇÈÚÇÏ äÇİĞÉ ÇááÚÈÉ 
screen = pygame.display.set_mode((800, 600))

# äÍÏíÏ ÕæÑÉ ÎáİíÉ ááÚÈÉ
background = pygame.image.load('background.png')

#ÊÍÏíÏ ÕæÊ ÇáÎáİíÉ İí ÇáÚÈÉ 
mixer.music.load("background.wav")
mixer.music.play(-1)

#ÇÎÊíÇÑ ÇÓã ÇáäÇİĞÉ æÕæÑÉ ááäÇİĞÉ 
pygame.display.set_caption("Space Invader")
icon = pygame.image.load('ufo.png')
pygame.display.set_icon(icon)

#ÊÍÏíÏ ÕæÑÉ ÔÎÕíÉ ÇáÇÚÈ
playerImg = pygame.image.load

#ÊÍÏíÏ ÇÍÏÇËíÇÊ ÙåæÑ ÇáÇÚÈ
playerX = 370
playerY = 480
playerX_change = 0
playerY_change = 0

#äÍÏíÏ åÏÏ ÑÕÇÕÇÊ ÇáÇÚÈ
bullet_state_counter = 10

# ãÕİæİÇÊ ÊãËá ÇÍÏÇËíÇÊ ãŞÚ ÇáÇÚÏÇÁ 
enemyX = []
enemyY = []
enemyX_change = []
enemyY_change = []

# ÊãËá ÕæÑ ÇáÔÎÕíÇÊ
enemyImg = []

# ÇáÚÏÏ ÇáÇİÕì ááÇÚÏÇÁ
num_of_enemies = 6

for i in range(num_of_enemies):
    # ÊÍÏíÏ ÕæÑÉ ÔÎÕíÉ ÇáÇÚÏÇÁ
    enemyImg.append(pygame.image.load('enemy.png'))
    # áÊÍÏíÏ ãæÇŞÚ ÇáÇÚÏÇÁ ÈÔßá ÚÔæÇÆí
    enemyX.append(random.randint(0, 736))
    enemyY.append(random.randint(50, 150))
    enemyX_change.append(4)
    enemyY_change.append(40)

# ÇÓÊÏÚÇÁ ÕæÑÉ ÇáÑÕÇÕÇÊ 
bulletImg = pygame.image.load('bullet.png')

# ÊÍÏíÏ ãæŞÚ ßÊÇÈÉ ÇáäŞÇØ
score_value = 0
font = pygame.font.Font('freesansbold.ttf', 32)

# ÇÙåÇÑ ÇáäŞÇØ İí ÇááÚÈÉ
def show_score(x, y):
    score = font.render("Score : " + str(score_value), True, (255, 255, 255))
    screen.blit(score, (x, y))
#ÇÙåÇÑ ÚÏÏ ÇáÑÕÇÕÇÊ
def show_bullet(x, y):
    score = font.render("Bullet: " + str(bullet_state_counter), True, (255, 255, 255))
    screen.blit(score, (x, y))

#ÇÙåÇÑ ÌãáÉ ÇäÊåÇÁ ÇááÚÈÉ
def game_over_text():
    over_text = over_font.render("GAME OVER", True, (255, 255, 255))
    screen.blit(over_text, (200, 250))

# ÊİíÑ ÇÍÏÇËíÇÊ ÇáÇÚÈ
def player(x, y):
    screen.blit(playerImg, (x, y))


# ÊİíÑ ÇÍÏÇËíÇÊ ÇáÇÚÏÇÁ
def enemy(x, y, i):
    screen.blit(enemyImg[i], (x, y))

# ÊÛíÑ ÇÍÏÇËíÇÊ ÇØáÇŞ ÇÑÕÇÕÉ ãÚ ÇÍÏÇËíÇÊ ÇáÇÚÈ
def fire_bullet(x, y):
    global bullet_state
    bullet_state = "fire"
    screen.blit(bulletImg, (x + 16, y + 10))

# ŞÊá ÇáÚÏæ İí ÍÇá ÇÕÊÏÇã ÇáÑÕÇÕÉ İí ÇáÚÏæ Çæ ÇäåÇÁ ÇáÚÈÉ İí ÍÇá ÇÕÊÏÇã ÔÎÕíÉ ÇáÇÚÈ ÈÇÍÏ ÇáÇÚÏÇÁ
def isCollision(enemyX, enemyY, bulletX, bulletY):
    distance = math.sqrt(math.pow(enemyX - bulletX, 2) + (math.pow(enemyY - bulletY, 2)))
    distance2 = math.sqrt(math.pow(enemyX - playerX, 2) + (math.pow(enemyY - playerY, 2)))

    if distance2 < 27:
        return 2
    if distance < 27:
        return 1
    else:
        return False

# ÇÓÊáÇã ÇæÇãÑ ÊæÌíå ãä ÇáÇÚÈ ááÊÍßã İí ÔÎÕíÉ ÇáÇÚÈ
if event.type == pygame.KEYDOWN:
          if event.key == pygame.K_LEFT:
                playerX_change = -5
          if event.key == pygame.K_UP:
                playerY_change = -5
          if event.key == pygame.K_DOWN:
                playerY_change = 5
          if event.key == pygame.K_RIGHT:
                playerX_change = 5
          if event.key == pygame.K_SPACE:
		if bullet_state is "ready" and bullet_state_counter > 0:
                    bulletSound = mixer.Sound("laser.wav")
                    bulletSound.play()
                    # Get the current x cordinate of the spaceship
                    bulletX = playerX
                    bulletY = playerY
                    fire_bullet(bulletX, bulletY)
                    bullet_state_counter -= 1

# ÇáÊÇßÏ ãä ÚÏã ÎÑæÌ ÔÎÕíÉ ÇáÇÚÈ ÎÇÑÌ ÍÏæÏ ÇáäÇİÏÉ
playerX += playerX_change
playerY += playerY_change
if playerX <= 0:
   playerX = 0
elif playerX >= 736:
   playerX = 736

# ÊÍÑíß ÇáÇÚÏÇÁ
 enemyX[i] += enemyX_change[i]
 if enemyX[i] <= 0:
            enemyX_change[i] = 4
            enemyY[i] += enemyY_change[i]
 elif enemyX[i] >= 736:
            enemyX_change[i] = -4
            enemyY[i] += enemyY_change[i]